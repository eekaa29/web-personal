{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "functions": {
    "api/**/*.py": {
      "maxDuration": 10,
      "excludeFiles": "{tests/**,__tests__/**,**/*.test.py,**/test_*.py,fixtures/**,__fixtures__/**,static/**,assets/**}"
    }
  },
  "installCommand": "uv venv --python 3.12 && uv pip install -r requirements.txt",
  "buildCommand": "uv run python manage.py tailwind build && uv run python manage.py collectstatic --noinput",
  "rewrites": [
    { "source": "/(.*)", "destination": "/api/index.py" }
  ]
}
